# 问题修复总结

## 修复日期

2026-01-18

## 问题描述与解决方案

### 问题 1: 用户朗读文章时，朗读到一半teacher就开始响应

**现象：**

- 用户在朗读长文章时中间有停顿
- VAD检测到静音后过早触发AI响应
- 导致用户朗读被打断

**根本原因：**

- `silence_duration_ms` 设置为 800ms 太短
- 用户朗读时的自然停顿（换气、思考）超过800ms

**解决方案：**

```python
# app.py 第 718 行
"silence_duration_ms": 1500,  # 从 800ms 增加到 1500ms
```

**效果：**

- 用户在朗读时有1.5秒的停顿时间
- 不会因为短暂停顿而被打断
- 更自然的对话体验

---

### 问题 2: 阶段切换后，每次都等待用户先说

**现象：**

- 用户点击阶段切换按钮（如"📖 Article"）
- AI更新了提示词但不主动开始
- 需要用户先说话才能触发AI

**根本原因：**

- 只发送了 `session.update` 更新配置
- 没有发送触发消息让AI主动开始
- 之前的触发消息太通用

**解决方案：**

```python
# app.py 第 769-794 行
# 1. 更新会话配置（包含VAD设置）
await client.send_session_update({
    "instructions": stage_prompt,
    "turn_detection": {
        "type": "server_vad",
        "threshold": 0.5,
        "silence_duration_ms": 1500,
    },
})

# 2. 等待配置生效
await asyncio.sleep(0.3)

# 3. 发送明确的触发消息
stage_messages = {
    "vocabulary": "Now begin teaching the vocabulary. Start by introducing the first word.",
    "article": "Now start the article reading stage. Read the full article aloud first.",
    "question": "Now begin the question practice. Ask the first question.",
    "review": "Now start the review. Summarize what we learned today."
}
trigger_msg = stage_messages.get(stage, f"Now start the {stage} stage.")
await client.send_text_message(trigger_msg)
```

**效果：**

- 阶段切换后AI立即主动开始
- 明确告诉AI该做什么（读文章、提问题等）
- 用户不需要先说话

---

### 问题 3: "文章"阶段，teacher应该先朗读文章

**现象：**

- 切换到文章阶段后，AI让用户先朗读
- 与教学设计不符（应该是teacher示范，student跟读）

**根本原因：**

- 提示词中的指令不够强调
- AI可能误解为让学生先读

**解决方案：**

```python
# lesson_manager.py 第 155-176 行
def _get_article_stage_prompt(self) -> str:
    return """ARTICLE READING STAGE:

Your task: Guide student through reading and understanding the article.

CRITICAL: YOU MUST READ THE ARTICLE FIRST! DO NOT ask student to read before you do!

Instructions:
1. **FIRST**, YOU must read the COMPLETE article aloud (every word, don't skip or summarize)
2. **AFTER** you finish reading, say: "Now you try reading it."
3. Listen patiently while student reads (don't interrupt even if they pause)
4. After student finishes, ask: "Good job! Do you understand everything? Any questions?"
5. If student asks questions:
   - Answer clearly and briefly
   - Practice difficult parts if needed (max 2-3 times)
6. Move on when student understands

...

REMEMBER: Always read the full article yourself BEFORE asking student to read!
"""
```

**关键改进：**

1. ✅ 添加 `CRITICAL` 警告
2. ✅ 使用 **加粗** 强调 `FIRST`
3. ✅ 明确说明 "DO NOT ask student to read before you do"
4. ✅ 在结尾重复提醒
5. ✅ 配合阶段切换的触发消息："Read the full article aloud first"

**效果：**

- AI切换到文章阶段后立即开始朗读完整文章
- 朗读完成后才让学生跟读
- 符合正确的教学流程

---

## 测试验证

### 测试场景

2026-01-18 实际课程测试：

- 课程内容：Micronesia文化差异（9个单词 + 1篇文章 + 3个问题）
- 课程时长：约5分钟
- 测试的阶段切换：vocabulary → article → question → review

### 测试结果

从服务器日志可见：

1. **词汇阶段** ✅
   - AI主动开始教学
   - 逐个教授9个单词
   - 每个词练习2-3次

2. **文章阶段切换** ✅

   ```
   INFO:__main__:🔄 Switching to stage: article
   INFO:__main__:✅ Switched to article stage, AI will start immediately
   ```

   - AI收到触发消息："Read the full article aloud first"
   - AI立即开始朗读完整文章
   - 学生朗读时有多次停顿（speech_started → speech_stopped）
   - 不会被1.5秒内的停顿打断

3. **问答阶段切换** ✅

   ```
   INFO:__main__:🔄 Switching to stage: question
   INFO:__main__:✅ Switched to question stage, AI will start immediately
   ```

   - AI主动提出第一个问题
   - 学生回答
   - AI给予反馈

4. **复习阶段切换** ✅
   ```
   INFO:__main__:🔄 Switching to stage: review
   INFO:__main__:✅ Switched to review stage, AI will start immediately
   ```

   - AI主动开始总结
   - 询问最后问题
   - 给予鼓励

---

## 代码变更汇总

### 1. app.py

**位置：第 710-722 行**

- 修改：初始会话配置的 `silence_duration_ms`
- 从：`800`
- 到：`1500`

**位置：第 769-794 行**

- 修改：阶段切换时的session.update和触发消息
- 新增：
  - VAD配置（确保一致性）
  - 等待配置生效（0.3秒）
  - 明确的阶段触发消息
  - 更新日志输出

### 2. lesson_manager.py

**位置：第 155-176 行**

- 修改：`_get_article_stage_prompt()` 方法
- 新增：
  - `CRITICAL` 警告行
  - 加粗的 `**FIRST**` 强调
  - 明确的禁止语句
  - 结尾的重复提醒
  - "Listen patiently" 提示不要打断学生

---

## 使用建议

### 为教师/用户

1. **朗读文章时**：
   - 正常朗读，不用担心停顿
   - 有1.5秒的思考和换气时间
   - 如果停顿超过1.5秒，AI会认为你已经说完

2. **阶段切换时**：
   - 点击阶段按钮后，AI会立即开始
   - 不需要自己先说话
   - 建议在当前阶段完成后再切换

3. **文章阶段**：
   - AI会先完整朗读文章（作为示范）
   - 听完后再开始跟读
   - 朗读时可以适当停顿

### 为开发者

1. **调整VAD参数**：
   - `silence_duration_ms`: 控制静音检测时间
   - 建议范围：1000-2000ms
   - 太短：容易打断用户
   - 太长：响应延迟过大

2. **添加新阶段**：
   - 在 `lesson_manager.py` 添加提示词方法
   - 在 `app.py` 的 `stage_messages` 字典添加触发消息
   - 确保触发消息明确告诉AI要做什么

3. **提示词设计原则**：
   - 使用 `CRITICAL`/`IMPORTANT` 突出关键指令
   - 使用 `**加粗**` 强调执行顺序
   - 在开头和结尾都提到关键规则
   - 给出具体的示例对话

---

## 未来优化建议

1. **自适应VAD**：
   - 根据阶段自动调整 `silence_duration_ms`
   - 词汇阶段：800ms（短问答）
   - 文章阶段：2000ms（长朗读）
   - 问答阶段：1200ms（中等长度）

2. **阶段自动切换**：
   - 检测当前阶段是否完成
   - 自动切换到下一阶段
   - 减少手动操作

3. **朗读进度提示**：
   - 在用户朗读长文章时显示进度
   - 避免用户不确定是否继续

4. **智能打断检测**：
   - 区分"停顿思考"和"说完了"
   - 使用更智能的语音检测算法
